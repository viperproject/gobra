// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

// This should actually be PR #810 (not "issue"), but the TestAnnotationParser
// cannot be easily extended to match against "pr" instead of issue below.
//:: IgnoreFile(/gobra/issue/810/)

package main

// ##(-I ./)
//:: ExpectedOutput(import_pre_error)
importRequires acc(&bar.B) && acc(&bar.C)
import "bar"