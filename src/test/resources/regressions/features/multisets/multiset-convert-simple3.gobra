// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

package pkg

func example1(ghost x int, ghost xs seq[int]) {
  assert x in set(xs) ==> x in mset(xs)
  assert x in mset(xs) ==> x in set(xs)
}
