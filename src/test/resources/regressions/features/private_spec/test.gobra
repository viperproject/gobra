// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

package pkg

type Pair struct {
  Left, Right, p int
}

type Test struct {
  L, R, p int
}

pred (s *Pair) PrivateMem() {
  acc(&s.p)
}

pred (s *Pair) PublicMem() {
  acc(&s.Left) && acc(&s.Right) && s.PrivateMem()
}

ensures acc(&result.Left) && acc(&result.Right) && result.PrivateMem()
//ensures this.PublicMem()
ensures /* unfolding this.PublicMem() in */ (result.Left == value.Left && result.Right == value.Right)
construct &Pair() {
  //fold result.PrivateMem()
  //fold this.PublicMem()
}

/* requires acc(&this.Left) && acc(&this.Right) && this.PrivateMem()
//requires this.PublicMem()
pure deref &Pair() {
  //return unfolding this.PublicMem() in unfolding this.PrivateMem() in *this
  return unfolding this.PrivateMem() in *this
} 

//requires acc(&this.Left) && acc(&this.Right) && this.PrivateMem()
requires this.PublicMem()
pure func Get(this *Pair) Pair {
  return *this
}

requires acc(&this.Left) && acc(&this.Right) && this.PrivateMem()
ensures  acc(&this.Left) && acc(&this.Right) && this.PrivateMem()
//requires this.PublicMem()
//ensures this.PublicMem()
ensures  a == *this // deref(this) == rhs
assign &Pair(a Pair) {
  //unfold this.PublicMem()
  unfold this.PrivateMem()
  *this = a // regular assignment to all fields including private and public ones
  fold this.PrivateMem()
  //fold this.PublicMem()
} */

ensures result.Left == value.Left && result.Right == value.Right
construct Pair()

func bad1(a, b Pair) {
  if a.Left == b.Left && a.Right == b.Right {
    assert a == b // should fail => OK
  }
}

requires acc(&a.Left) && acc(&a.Right)
func bad2(a *Pair) {
  *a = Pair{} // fails, not enough permission
}

requires acc(&a.Left) && acc(&a.Right)
func bad3(a *Pair) {
  x := *a // fails, not enough permission
}


func main() {
  x := &Pair{Left: 2, Right: 0, p: 1}
  t := &Test{L: 2, R: 0, p: 3}
  var t2 = Test{L: 2, R: 0, p: 3}
  y := new(Pair)
  var z = Pair{Left: 2, Right: 3, p: 1}
}



