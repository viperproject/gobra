package main

type Test struct {
    a, b int
}

type Node struct {
    a, b, c int
}

type Tree adt {

    Node{
        value int
        left, right Tree
    }

    Leaf{}
}

func main() {

    structTest := Node{1, 2, 3}
    val := structTest.a
    test := Tree.Node{0, Leaf{}, Leaf{}}
    test2 := Tree.Node{20, test, test}
}
