// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

package main

func lenTestFail() {
	h1 := "hello"
	//:: ExpectedOutput(assert_error:permission_error)
	assert len(h1) == 4
}

func compareStr() {
	//:: ExpectedOutput(assert_error:permission_error)
	assert "a" < "bb" || "a" < "bb" || "a" == "bb"
}