// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

package termination

// note that by unsuccessful inference the method not terminate error with reason:
// "Inferred measures cannot verify the termination. Please provide one by yourself" is reported

ghost
decreases infer
func sign5(x int) int {
  if x == 0 {
  
     //:: ExpectedOutput(method_termination_error)
    return sign5(x)
  } else {
  
    if 1 < x {
      return sign5(x - 1)
    } else {
    
    if( x < -1 ) {
      return sign5(x + 1)
    } else {
      return x
     }
   }
  }
}








