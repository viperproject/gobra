// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

package main

// ##(-I ./)
import bar "bar"

func main() {
    x := &bar.Pair{Left: 2, Right: 0}
    
    //:: ExpectedOutput(error)
    y := &bar.pair{Left: 2, Right: 0} 

    bar.Foo(1,1,x)

    //:: ExpectedOutput(error)
    assert(bar.pf(2) == 2)

    assert(bar.PF(2) == 2)

    //:: ExpectedOutput(error)
    bar.foo(1,1,nil) 

    assert(acc(x) && x.Left == 2)

    //:: ExpectedOutput(error)
    assert(bar.p(0))

    inhale(bar.P(0))
    assert(bar.P(0))

    //:: ExpectedOutput(error)
    assert(x.p == 3)

    assert(bar.G == 42) 

    //:: ExpectedOutput(error)
    assert(bar.g == 2) 
}
