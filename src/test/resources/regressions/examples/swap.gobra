// Any copyright is dedicated to the Public Domain.
// http://creativecommons.org/publicdomain/zero/1.0/

package pkg

/*
pred p(x *int) {
    acc(x) && (*x >= 5 ==> low(*x) && *x >= 6)
}


requires acc(x)
ensures acc(x)
ensures  *x < 5 ==> *x == old(*x)
ensures  *x >= 5 ==> low(*x)
func example1(x *int) {
  if *x >= 5 {
    *x = 9
  }
}

func example2(x *int) {
    inhale p(x)
    unfold p(x)
    assert *x >= 5 ==> low(*x)
    fold p(x)
    exhale p(x)
}

pred unary(*int) {
    true
}

pred q(x *int) {
    unary(x) && p(x)
}
*/